This script will move a folder full of images into folders based off of their respective creation dates.

How to use:
  - Drag and Drop the parent folder with the images onto `runMoveIntoDateFolder.bat`.
